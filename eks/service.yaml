apiVersion: v1
kind: Service
metadata:
  name: vue-app
  labels:
    app: vue-app
spec:
  type: LoadBalancer
  selector:
    app: vue-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
